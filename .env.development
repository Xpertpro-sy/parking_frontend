# Essai gratuit : durée en minutes (tests d’expiration). Commenter la ligne pour revenir à 1 mois calendaire en `npm run dev`.
# En `npm run build` (production), ce fichier n’est pas chargé → essai = 1 mois par défaut.
VITE_TRIAL_MINUTES=1
